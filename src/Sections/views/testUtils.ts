import { StoryConfig } from '../../typings/story-config';

export const storyConfigEmpty: StoryConfig = {
  metadata: { displayName: '', props: {} },
  config: { importFormat: '', moduleName: '', repoBaseURL: '' },
  component: null,
};
